set -g status-left-length 100
set -g status-right-length 100

set -g status-interval 1 # for ticking seconds

bind l run-shell "sesh last"
bind s display-popup -E -w 85% -h 90% -d "#{pane_current_path}" -T " TV channel " tv sesh
